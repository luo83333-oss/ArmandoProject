# NL2-157 生产 cron 示例（ECS 上 crontab -e）
# 每日 03:15 备份，保留 14 天（脚本内 RETENTION_DAYS 控制）
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

15 3 * * * cd /opt/ArmandoProject && RETENTION_DAYS=14 ./deploy/backup/backup-mysql.sh >> /var/log/market-mysql-backup.log 2>&1
